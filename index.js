import HashMap from "./HashMap.mjs";
import HashSet from "./HashSet.mjs";

const hashmap = new HashMap();
const hashset = new HashSet();

/**
 * Functionalities
 * set / get / has / remove
 * length
 * clear
 * keys / values / entries
 * table growth corresponding to the load factor
 */
